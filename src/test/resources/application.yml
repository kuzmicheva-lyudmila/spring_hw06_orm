spring:
 datasource:
  url: jdbc:h2:mem:testdb
  #schema: test-app-schema.sql
  data: test-data.sql
 jpa:
  generate-ddl: true
  hibernate:
   ddl-auto: create-drop
  show-sql: true

logging:
 level:
  ROOT: ERROR